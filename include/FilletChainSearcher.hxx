#pragma once

// Compatibility shim for the trailing example in the original
// D:/workspace/cad-algorithm/识别圆角.cpp. The class is defined earlier in that
// same source file; this header deliberately adds no alternative implementation.

