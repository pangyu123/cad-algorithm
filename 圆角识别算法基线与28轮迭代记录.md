# OCCT 7.8.1 圆角识别算法：基线、28 轮迭代记录与后续接手说明

> 文档用途：这是后续新对话继续修改算法时的单文件交接基线。新对话应先完整阅读本文件，再阅读目标轮次的详细报告和代码。  
> 基线状态：28 轮既定迭代已完成，当前版本定位为“工业化候选基线”，不是已经通过企业真实模型验收的最终量产版。  
> 记录日期：2026-08-31。

## 1. 必须保留的基线约束

1. 原始算法基线是 `D:\workspace\cad-algorithm\识别圆角.cpp`。工程通过 `src/FilletBaselineAdapter.cpp` 直接编译、调用并转换该源码的结果；适配器不应复制或改写原算法规则。
2. 后续改进必须继续建立在“识别圆角.cpp 已集成”的工程上。早期生成的 `FilletSearcher` 已弃用，不能重新作为基线。
3. 改进算法实现位于 `src/ImprovedFilletRecognizer.cpp`，公开接口位于 `include/fillet/ImprovedFilletRecognizer.hpp`。
4. 算法核心输入始终是 `TopoDS_Shape`。STEP、IGES、BREP 只是进入该输入前的文件读取方式；文件格式可能导致导入后的曲面表示、缝合状态、边共享关系、容差和 pcurve 不同，因此会间接影响识别结果，但识别器本身不是按扩展名执行不同规则。
5. OCCT 固定为 7.8.1，Visual Studio 固定为 VS2019/MSVC x64，工程采用 C++17。
6. 依赖、OCCT SDK、构建目录、测试模型和报告继续放在 D 盘，不向 C 盘安装依赖。C 盘当前只保存本工作区源码。
7. 未经独立人工真值验证，不能把“候选面/链数量增加”写成“准确率提高”。报告中的 `validated` 是现有证据体系下通过，不等于企业业务真值认证。

## 2. 环境、工程和数据位置

| 内容 | 路径 |
|---|---|
| 当前工程源码 | `C:\Users\Administrator\Documents\ChatGPT\cad-algorithm` |
| 原始基线源码 | `D:\workspace\cad-algorithm\识别圆角.cpp` |
| OCCT 7.8.1 源码 | `D:\workspace\cad-algorithm-deps\occt-src-7.8.1` |
| OCCT 7.8.1 构建目录 | `D:\workspace\cad-algorithm-deps\occt-build-7.8.1` |
| OCCT 7.8.1 SDK | `D:\workspace\cad-algorithm-deps\occt-7.8.1` |
| OCCT Debug DLL | `D:\workspace\cad-algorithm-deps\occt-7.8.1\win64\vc14\bind` |
| OCCT 自带数据模型 | `D:\workspace\cad-algorithm-deps\occt-src-7.8.1\data` |
| Debug 构建目录 | `D:\workspace\cad-algorithm-build\debug` |
| Debug 可执行程序 | `D:\workspace\cad-algorithm-build\debug\Debug\fillet-search.exe` |
| 自动生成测试模型 | `D:\workspace\cad-algorithm-build\debug\test-artifacts` |
| 各轮机器可读报告 | `D:\workspace\cad-algorithm-reports\iteration1` 至 `iteration28` |
| 最新完整性能基线 | `D:\workspace\cad-algorithm-reports\iteration28` |
| 性能门失败负测 | `D:\workspace\cad-algorithm-reports\iteration28-budget-failure` |

注意：`CMakePresets.json` 中虽然存在 `windows-release` preset，但当前 D 盘 OCCT SDK 是 Debug 构建。正式 Release 性能评估前，应先在 D 盘建立与 VS2019 匹配的 OCCT 7.8.1 Release SDK，不能用现有 Debug 耗时冒充量产 SLA。

## 3. 当前工程结构与数据流

核心文件：

- `src/FilletBaselineAdapter.cpp`：调用原始“识别圆角.cpp”的 baseline 适配层。
- `src/ImprovedFilletRecognizer.cpp`：28 轮增强后的主体实现。
- `include/fillet/ImprovedFilletRecognizer.hpp`：Options、Face、Link、Chain、Path、RadiusProfile、Feature、性能诊断等公开结构。
- `src/RecognitionReport.cpp`：JSON、CSV、DOT、BREP 审计报告输出。
- `src/TruthEvaluation.cpp`、`include/fillet/TruthEvaluation.hpp`：面/链/Feature 三级真值评测。
- `app/main.cpp`：STEP/IGES/BREP 导入、命令行参数、A/B、Sewing、真值和性能门入口。
- `tests/FilletBaselineTests.cpp`：生成正负样件并执行整体回归。
- `truth/`：真值格式说明和当前确定性样件标签。
- `docs/iteration-N-results.md`：每轮的原始详细记录；本文件是其交接摘要，不替代这些证据。

主要数据流：

```text
STEP / IGES / BREP
        ↓ 导入
   TopoDS_Shape
        ├─ baseline::search → 原始“识别圆角.cpp”结果
        └─ improved::recognize
             ↓ 模型诊断与尺度/容差
             ↓ 逐面几何分类、标准曲面恢复、曲率场与支撑恢复
             ↓ Link 图、Chain、最大非分支 Path、拓扑角色
             ↓ 主方向、R(s)、跨种子稳定性、全局方向优化
             ↓ Feature 三态证据与置信度
             ↓ 报告、真值评测、性能预算
```

## 4. 构建、测试和常用运行命令

在工程根目录执行：

```powershell
cmake --preset windows-debug
cmake --build --preset debug
ctest --preset debug
```

`ctest --preset debug` 会显式把 OCCT 7.8.1 Debug DLL 目录加入 PATH，避免“无法定位程序输入点”或错误加载系统中另一套 OCCT DLL。最近一次完整回归为 1/1 通过，约 13.77 秒。

常用运行方式：

```powershell
# 默认运行改进算法
D:\workspace\cad-algorithm-build\debug\Debug\fillet-search.exe model.step

# 原始基线与改进算法 A/B
D:\workspace\cad-algorithm-build\debug\Debug\fillet-search.exe --compare model.step

# 报告写入 D 盘
D:\workspace\cad-algorithm-build\debug\Debug\fillet-search.exe --improved `
  --report-dir D:\workspace\cad-algorithm-reports\manual-run model.brep

# 显式进行原始与分区 Sewing 副本对比；正常识别不会自动 Sewing
D:\workspace\cad-algorithm-build\debug\Debug\fillet-search.exe --compare-sewing `
  --sewing-factor 1 --report-dir D:\workspace\cad-algorithm-reports\sewing-check model.iges

# 真值评测
D:\workspace\cad-algorithm-build\debug\Debug\fillet-search.exe --improved `
  --truth-file labels.csv --truth-iou 0.5 `
  --report-dir D:\workspace\cad-algorithm-reports\truth-check model.brep

# 性能预算：超预算仍写完整报告，随后返回非零退出码
D:\workspace\cad-algorithm-build\debug\Debug\fillet-search.exe --improved `
  --performance-budget-ms 30000 --fail-on-performance-budget `
  --report-dir D:\workspace\cad-algorithm-reports\budget-check model.step
```

## 5. 半径参数的准确含义

输入半径不是“变半径圆角唯一半径”。`minimumRadius` 和 `maximumRadius` 是候选过滤区间，具体含义由 `RadiusFilterMode` 决定：

- `NominalWithinRange` / CLI `nominal`：面的名义半径落入区间；默认模式。
- `EntireProfileWithinRange` / `entire`：该面采样到的完整半径范围都在区间内。
- `ProfileIntersectsRange` / `intersects`：采样半径范围与查询区间有交集。
- `AnyRadius` / `any`：关闭半径过滤，先识别全部候选。

变半径结果另外通过路径级 `RadiusProfile` 表达，包含有序 `R(s)`、最小/最大/起止半径、相邻跳变、跨种子稳定性及 `constant`、`smooth-variable-candidate`、`segmented`、`discontinuous` 或 `insufficient-evidence` 行为。BSpline 半径来自主曲率采样，并非 CAD 建模历史中保存的精确半径律。

## 6. 28 轮逐轮技术点与达到的效果

### 第 1 轮：工程集成、诊断和标准曲面恢复

- 技术点：保持原始“识别圆角.cpp”不变；加入 BRep 有效性、尺度、容差、退化/非流形诊断，自适应线性容差，`ShapeAnalysis_CanonicalRecognition` 圆柱/球面恢复，双切向支撑验证，以及逐面来源、gap、支撑与置信度。
- 效果：CrankArm 从 0 面恢复到 4 面，Motor 从 3 到 16，hammer BREP 从 0 到 3；同时暴露 bottle 大半径主体曲面疑似误报和 IGES 有几何却缺拓扑支撑的问题。此轮证明“标准曲面恢复可补召回”，未证明准确率。

### 第 2 轮：一般自由曲面曲率场和主体面风险门

- 技术点：对 BSpline/Bezier 网格采样主曲率，用半径变异系数、次/主曲率比筛 canal-like 曲面，记录局部半径范围、变半径候选和初步凹凸性；组合角度覆盖、面积比、半径/模型尺度过滤主体曲面。
- 效果：第一轮 bottle 的 4 个可疑大半径面被滤掉；CrankArm 增至 16 面、Pump_TopCover 得到 2 个、Motor 得到新的自由曲面候选。bearing/hammer IGES 仍因支撑拓扑失败而为 0。

### 第 3 轮：断裂导入模型的几何支撑恢复与复杂路径分解

- 技术点：对不共边边界使用包围盒邻域、边中点投影、曲面 gap 和法向夹角推断切向支撑；加入每面/每模型预算；把候选图分解为最大非分支路径、闭环和分叉路径。
- 效果：bearing IGES 从 0 到 41 个候选面，hammer IGES 从 0 到 15；推断证据置信度限定为较低等级。bottle 的 24 面网络产生 105 条路径，暴露复合连接过连问题。

### 第 4 轮：可审计、可复现报告与稳定几何指纹

- 技术点：加入量化几何 `persistent_id`；导出 summary、faces、links、chains、paths、DOT、accepted-faces BREP 和真值模板；CLI 增加 `--report-dir`。
- 效果：bottle、bearing、hammer 各运行两次，共 24 组文件 SHA-256 全部一致，建立了同一 OCCT 导入流程下的确定性回归闭环。`persistent_id` 是评测指纹，不是跨重参数化/缩放的永久 GUID。

### 第 5 轮：外部结构支撑与伪复合连接清理

- 技术点：区分全部切向邻面 `supportFaceIds` 与非候选结构母面 `externalSupportFaceIds`；复合补链只允许两个共同外部结构支撑；增加链级验证状态。
- 效果：bottle 删除 48 条伪复合连接，路径从 105 降为 25而 3 条主链不变；bearing 链更保守地从 32 调整为 37，hammer 的伪二面链被拆开。链路不再让圆角面互相充当母面。

### 第 6 轮：拓扑角色和复合 Feature 语义层

- 技术点：按图度数定义 Isolated/Terminal/Band/Junction Patch；将连通链聚合为 IsolatedPatch、SimpleChain、ClosedLoop、CompositeJunctionNetwork Feature；修复 OCCT 缝边祖先重复导致的自环。
- 效果：bottle 底层仍有 25 条路径，但工程语义归并成 3 个圆角 Feature，其中两个是 24 面复合汇合网络；路径数量不再被误当成圆角特征数量。

### 第 7 轮：双曲率 BSpline 角部和几何—拓扑角色校验

- 技术点：除带状单主曲率模式外，支持两主曲率显著且同号的双曲率角部；用几何类型验证 Junction/Band 角色，冲突降置信度并可审计。
- 效果：bottle 的带状面和汇合面几何角色通过；bearing 的两个高图度面被识别为带状几何冲突；CrankArm 新增两个约 R=6.39 的双曲率候选，但仍需真值确认。

### 第 8 轮：公共边中点的脊线方向连续性

- 技术点：在共享边两侧计算次曲率主方向，以 `abs(dot)` 和默认 15° 阈值检查无向连续性；Link 增加是否进入图，Feature 聚合方向证据。
- 效果：bottle 8 条有效 Link、CrankArm 4 条有效 Link 均近似 1.0；修复未进入最终图的低对齐邻接污染 Feature 汇总的问题。IGES 无共享边时仍无方向证据。

### 第 9 轮：沿边多点方向采样与脐点保护

- 技术点：共享边默认取 5 个内部样本，统计覆盖率、最小/平均/标准差；有效覆盖至少 60%；两主曲率接近的近脐点样本跳过。
- 效果：bottle 40/40、CrankArm 20/20 样本有效且无方向冲突，长边局部扭转不再可能被单个中点或均值掩盖。

### 第 10 轮：无共享边复合连接的投影方向证据

- 技术点：对已经满足空间邻近、半径相容和双外部支撑的无共享边连接，从裁剪边界双向采样并投影到另一曲面，检查 gap、UV 裁剪状态、曲率和脐点；引入 `ProjectedBoundary` 证据源。
- 效果：bearing 6 条复合连接均获得 5/5 样本，其中 face 45–67 出现一个近 0 alignment 的稳定可疑点，Feature 置信度降至 0.56但不断链。

### 第 11 轮：方向异常的空间/UV 定位

- 技术点：逐样本保存两侧 XYZ、UV、gap、alignment 和曲率比；新增 `direction-samples.csv`、冲突点 BREP。
- 效果：定位 bearing 45–67 异常点，证明两侧空间几乎重合且 gap 仅约 `3.9e-9`，更像局部主方向旋转/分支变化，而不是错误远距离投影。

### 第 12 轮：冲突区加密与主方向矢量可视化

- 技术点：初始 5 点冲突后自动加密到 21 点，合并原始与加密证据；保存两侧主方向 XYZ；新增冲突矢量 BREP。
- 效果：bearing 45–67 得到 26/26 有效样本、7 个冲突，确认异常集中于参数区间而非单点噪声；仍保留连接并明确复核。

### 第 13 轮：受控 Sewing A/B

- 技术点：新增 `compareWithSewing()`，只在显式启用时对 Shape 副本 Sewing；输出原始/修复结果、面映射、自由/连续/多重边、删除面和 sewn BREP；原 Shape 不被替换。
- 效果：bearing 候选由 41 增到 153，而 hammer 从 15 降到 0，证明 Sewing 对不同导入模型影响可能完全相反，不能作为静默预处理或按候选数量自动择优。

### 第 14 轮：按 Solid/Shell 分区 Sewing 与安全门

- 技术点：Solid 优先、Shell 次之，散面按空间连通分区；记录自由边长度、壳/闭壳变化、未映射/一对多面映射并建立拓扑安全门。
- 效果：Motor 的 17 个壳和 CrankArm 的 2 个壳保持隔离，正常模型修复前后候选不变；IGES 即使安全门通过也仍有巨大候选变化，再次确认“拓扑安全”不等于“识别准确”。

### 第 15 轮：明确半径查询语义和路径半径轮廓

- 技术点：实现 nominal、entire、intersects、any 四种半径过滤；每条 Path 生成 RadiusProfile，Feature 聚合轮廓；新增半径 CLI 和 CSV。
- 效果：默认模式不改变上一轮拓扑；bottle 用 `2.4..2.6 + entire` 只保留 R=2.5 主链，`any` 能完全忽略数值区间；同时修复 ctest/F5 加载错误 OCCT DLL 的入口点问题。

### 第 16 轮：从主方向恢复有序 R(s)

- 技术点：在自由曲面选脊线主参数方向，沿中部行密集采样 11 点，按三维弧长归一化，并跨面按端点距离定向；用总变差、有效变化步、单调性和主导步比例分类半径行为。
- 效果：OCCT 生成 R=3→8 模型识别为 1 面/1 路径、11 点单调 `smooth-variable-candidate`，采样约 3.115→7.887；识别出“大变化幅度不等于断跳”。

### 第 17 轮：主方向流线积分与轨迹质量门

- 技术点：将固定 U/V 行改为主方向在 UV 中的双向二阶积分；失败才参数线回退；轨迹与主方向最小对齐低于 0.90 时禁止进入 R(s) 分类。
- 效果：R=3→8 样件流线覆盖 1.0、最小对齐约 0.99999；linkrods 之前 5 条 segmented 被发现是对齐仅约 0.035 的采样方向误报，修正为恒定/证据不足。

### 第 18 轮：自适应多种子流线和尺度不变性

- 技术点：最多 5 种子、失败缩步、长度覆盖率、步级方向门；UV 度量奇异性改为无量纲判断；积分停机不再错误使用模型导入容差。
- 效果：bearing 的 128 个可追踪自由面全部走流线、零参数线回退；linkrods 最小流线对齐提升到约 0.98；整体缩放到 `1e-4` 的 R=3→8 模型仍通过，消除两处尺度相关错误。

### 第 19 轮：跨圆角宽度的多流线稳定性

- 技术点：默认要求不同横向行的 3 条有效流线，统一方向后重采样到 11 个共同位置，默认最大跨种子相对偏差 10%；不稳定的非恒定结论降为证据不足。
- 效果：R=3→8 样件 3 种子稳定并保持平滑变半径；bearing 面 78 偏差约 0.7957，对应路径降级；linkrods 面 24 偏差约 0.6271但没有被错误升级为变半径。

### 第 20 轮：路径规范方向和全局动态规划

- 技术点：分叉路径统一由分叉点向外，开放路径和闭环用稳定 ID 规范化；以端点距离和有向主方向误差为代价，用二状态 DP 全局决定各流线是否翻转，闭环包含首尾闭合成本。
- 效果：19 条多流线轮廓执行全局优化，翻转 21 个面；全局成本保证不高于贪心；47 条触及分叉路径均满足规范方向。闭环分支此时实现但尚缺真实运行覆盖。

### 第 21 轮：闭环、多分支确定性真值件与精确距离门

- 技术点：生成四面闭合 BSpline 圆角环和全 12 边圆角分支箱；复合连接在包围盒预筛后增加 `BRepExtrema_DistShapeShape` 精确修剪面距离门。
- 效果：闭环得到 4 面、4 活动连接、1 链、1 闭合路径、44 个 R(s) 样本且 R=5；修复周期底层曲面宽包围盒把四节点环误连成 K4 的缺陷。分支样件得到 14 面、3 链、10 路径。

### 第 22 轮：逐转移方向证据和失败原因

- 技术点：将路径总成本拆成逐连接证据，记录归一化 gap、有向点积、成本、闭环 closure 和 `gap-too-large`、`direction-mismatch`、`unobserved-intermediate-faces` 等原因。
- 效果：闭环 4 条转移全部通过；CrankArm 有 4 通过、1 个中间面缺证；linkrods 有 7 通过、8 个中间面缺证。强制阈值回归可稳定触发 gap 和方向失败分支。

### 第 23 轮：Feature 三态证据与公开置信度公式

- 技术点：拓扑/支撑、半径稳定性、路径方向分别输出 `validated`、`conflict`、`insufficient-evidence`；总态取最严重。最终置信度 = 基础几何置信度 × 加权三态分数，默认权重 0.40/0.30/0.30，状态分数 validated=1、insufficient=0.65、conflict=0.35。
- 效果：闭环样件成为三项全通过的自由曲面多面 Feature，置信度 0.9；bottle 有 2 个通过、1 个证据不足；bearing 明确区分 3 个冲突和 34 个证据不足，不再用一个模糊分数混淆冲突与缺证。

### 第 24 轮：解析中间面的方向桥接

- 技术点：当两条自由面流线之间是圆柱、恢复圆柱或圆环面时，沿路径逐跳使用已有 Link 方向证据；全部兼容才通过，任一冲突明确失败，缺证保持未知。
- 效果：新增混合解析/BSpline 闭环，1 个两跳桥接通过，Feature 置信度 0.925；CrankArm 的一个缺证连接补齐；linkrods 的 8 段不被盲目放行，而是全部暴露为方向冲突。

### 第 25 轮：修剪缺口、错位、薄片负样本与数值分辨率门

- 技术点：增加边长/模型对角线默认 `1e-8`、面积/对角线平方默认 `1e-12` 的尺度归一门；短共享边 Link 不进图且禁止被复合连接绕过；薄片面明确拒绝。
- 效果：修剪缺口环必须断开为开放链；平移 0.25 的错位环拆为 1 面和 3 面两链；薄片样件严格门下 0 链；强制短边门把闭环拆成 4 个单面链。默认阈值不改变正常语料。

### 第 26 轮：解析桥的有符号代价进入全局 DP

- 技术点：沿“自由面—Link—解析面—Link—自由面”传播无向主方向轴的符号，圆柱/圆环内部用 12 步 UV 主方向场传播；桥接局部 gap 与有符号 alignment 直接进入 DP，缺证时才回退端点成本。
- 效果：混合闭环桥 alignment 0.996257，总方向成本约 0.64456→0.088671；CrankArm 桥 alignment 0.999056；linkrods 完整桥 alignment 近零且仍保持冲突，没有被全局翻转掩盖。

### 第 27 轮：面、链、Feature 三级真值评测

- 技术点：真值模板覆盖模型全部面，以 `persistent_id` 标记 FILLET/NOT_FILLET/UNKNOWN；面级混淆矩阵，链/Feature 按面集 IoU 默认 0.5 做一对一最大匹配；部分标注时禁用误导性的对象级指标。
- 效果：确定性闭环样件面/链/Feature 三级 F1 均为 1，R=3→8 样件三级也均为 1；这验证了评测链路和两个构造样件，不代表真实工业模型达到 100%。七个真实/OCCT 模型模板仍为 UNKNOWN，等待 CAD 工程师盲标。

### 第 28 轮：阶段计时、Link 索引缓存与性能预算

- 技术点：recognize 内部拆成诊断、逐面分析、拓扑、半径/方向、Feature 五阶段计时；建立面对到 Link 列表的索引；加入完成后性能预算和 CLI 失败门，超限仍保留完整结果。
- 效果：linkrods 的 238 次面对查询从理论扫描 9996 个 Link 降到访问 434 个局部候选，减少 95.7%；11 个模型建立 Debug 性能基线；极小预算负测正确返回退出码 1并保留报告。当前瓶颈被定位在支撑恢复和拓扑/精确面距，而非最后的 R(s) 聚合。

## 7. 当前回归模型与结果边界

### 7.1 确定性生成模型

| 模型 | 用途 | 当前关键结果 |
|---|---|---|
| `generated-variable-fillet.brep` | OCCT 构造 R=3→8 变半径 | 1 面、1 链、1 路径；3 个横向种子稳定；`smooth-variable-candidate`；真值三级 F1=1 |
| `generated-closed-bspline-fillet-ring.brep` | 四面自由曲面闭环 | 4 圆角面、1 闭环 Feature、44 样本、R=5；三态 validated、置信度 0.9；真值三级 F1=1 |
| `generated-mixed-analytic-bspline-fillet-ring.brep` | 解析/自由曲面混合闭环 | 1 个解析桥进入优化，signed alignment 0.996257；Feature validated、置信度 0.925 |
| `generated-branched-fillet-network.brep` | 多分叉圆角网络 | 14 面、3 链、10 路径；8 条路径触及分叉 |
| `generated-trim-gap-bspline-fillet-ring.brep` | 修剪缺口负样本 | 不允许形成闭环，保持开放链和证据不足 |
| `generated-offset-bspline-fillet-ring.brep` | 空间错位负样本 | 错位面与其余三面拆链 |
| `generated-sliver-fillet-patch.brep` | 薄片/数值分辨率负样本 | 严格门控下全部拒绝，0 Feature |

确定性真值文件：

- `truth/generated-closed-bspline-fillet-ring.csv`
- `truth/generated-variable-fillet.csv`

### 7.2 当前真实/OCCT 模型输出（不是准确率）

第 23–28 轮稳定的 Feature 三态摘要：

| 模型 | validated | conflict | insufficient | 平均置信度 | 解释 |
|---|---:|---:|---:|---:|---|
| bottle | 2 | 0 | 1 | 0.9533 | 两个解析复合网络证据完整；单面特征缺上下文 |
| CrankArm | 0 | 0 | 10 | 约 0.5900 | 解析桥已补齐一段，拓扑角色仍缺证 |
| bearing IGES | 0 | 3 | 34 | 0.4703 | 分别存在角色、跨种子半径、方向冲突；大量孤立/缺证 |
| hammer | 0 | 0 | 15 | 0.3211 | 大量依赖导入断裂后的几何支撑，需人工复核 |
| screw | 0 | 0 | 0 | 0 | 曲率候选无法通过支撑门；不能假定模型没有圆角 |
| linkrods | 0 | 2 | 4 | 0.7072 | 解析桥暴露 8 个方向冲突，并有跨种子不稳定 |
| Motor-c | 0 | 0 | 27 | 0.4915 | 多组件模型，多数 Feature 证据不足 |

这些模型的真值模板均覆盖全模型面，但当前保持 UNKNOWN。因此上表只能用来做回归和人工复核，不能用来发布 precision/recall。

## 8. 第 28 轮 Debug 性能基线

计时仅覆盖 `improved::recognize()`，不含文件读取和报告写入：

| 模型 | 总耗时 ms |
|---|---:|
| closed BSpline ring | 1,594.51 |
| mixed analytic/BSpline ring | 1,548.08 |
| variable fillet | 140.84 |
| branched network | 221.75 |
| bottle | 6,506.25 |
| CrankArm | 10,606.76 |
| bearing | 28,635.48 |
| hammer | 14,638.87 |
| screw | 522.69 |
| linkrods | 14,078.74 |
| Motor-c | 18,846.64 |

bearing 已接近演示用 30 秒预算。bottle、CrankArm、hammer、Motor 的主要时间在拓扑阶段；bearing、linkrods 还在逐面支撑恢复中消耗较多。后续优化优先考虑支撑恢复空间索引和复合连接精确面距调用。当前没有并行执行 OCCT 几何/拓扑运算，因为 Handle 共享、惰性缓存、线程安全与确定性尚未验证。

## 9. 报告体系

每个模型目录通常包含：

- `summary.json`：模型、识别、三态、计时和预算摘要。
- `performance.csv`：五阶段耗时、几何支撑测试和缓存工作量。
- `faces.csv`：逐面几何、半径、角色、支撑、拒绝原因和 persistent_id。
- `links.csv`：共享边/复合连接、G1、半径、方向、gap 及是否进入图。
- `chains.csv`、`paths.csv`、`features.csv`：三个聚合层级。
- `radius-profiles.csv`、`face-radius-samples.csv`、`radius-profile-samples.csv`：R(s) 证据。
- `radius-orientation-transitions.csv`、`direction-samples.csv`：逐连接方向证据。
- `graph.dot`、`accepted-faces.brep`、轨迹/冲突点/矢量 BREP：可视化复核。
- `truth-template.csv`：覆盖全部面的标注模板。
- 真值模式下的 `evaluation-summary.csv`、`evaluation-coverage.csv`、`evaluation-matches.csv`。

所有阈值调整都应保留新的报告目录，并与 `iteration28` 做机器可读 A/B；不要只截图或口头描述结果。

## 10. 当前已知限制和下一阶段优先级

1. **最大缺口是真实工业真值。** 应由 CAD 工程师对真实业务模型盲标，最好双人交叉复核，再按解析面、自由曲面、变半径、闭环、分叉和导入格式分层统计 precision/recall/F1。
2. **现有 BSpline R(s) 是微分几何估计。** 它不是建模历史恢复，也不是对所有曲面都能还原精确设计半径律；`smooth-variable-candidate` 必须保留候选语义。
3. **支撑恢复和复合连接仍是主要误报/性能风险。** 尤其 IGES 散面；Sewing 只能显式 A/B，不能自动采用。
4. **孤立面、端部面和复杂分叉仍可能缺几何角色证据。** 三态体系会诚实输出 insufficient，但业务系统需决定复核策略。
5. **性能仍是 Debug 基线。** 先建立 D 盘 Release SDK/构建，再定正式 SLA；之后才能评价优化和并行化。
6. **确定性范围有限。** 同一导入流程重复运行已验证稳定；大幅拓扑修复、重参数化、单位缩放可能改变 persistent_id。
7. **文件格式的影响发生在导入结果。** 调试 STEP/IGES 差异时，应先比较导入后 TopoDS_Shape 的面/边/壳、共享关系、容差、pcurve 和 Sewing 诊断，不应在识别核心中按文件扩展名硬编码。
8. **性能预算是完成后门控。** 它不做中途取消；硬实时应由独立进程隔离和超时控制承担。

建议后续顺序：

1. 完成真实业务模型真值标注和分层评测；
2. 用误报/漏报样本驱动阈值校准或新增几何证据，禁止只按候选数量调参；
3. 为支撑恢复建立空间索引，减少精确面距候选对；
4. 建立 Release OCCT 7.8.1 基线并定义 SLA；
5. 在线程安全和确定性验证后，再考虑并行逐面分析；
6. 完成宿主 API、异常策略、日志、版本化配置和部署验收。

## 11. 后续新对话的接手流程

把本文件提供给新对话后，应要求按以下顺序工作：

1. 先确认读取了本文件，并明确承诺继续以“识别圆角.cpp”为 baseline，不恢复旧 FilletSearcher。
2. 检查工程、OCCT SDK、Debug EXE 和 `iteration28` 报告路径是否仍存在。
3. 先运行 `ctest --preset debug`，确认当前基线 1/1 通过；若 DLL 入口点报错，检查是否加载了 D 盘 OCCT 7.8.1 Debug DLL。
4. 对待调整模型先用当前版本生成独立的 before 报告；如果是 STEP/IGES 差异，同时记录导入拓扑诊断和可选 Sewing A/B。
5. 说明本轮要解决的具体误报、漏报、链路错误、R(s) 错误或性能问题，并确定可验证断言；最好先把问题模型或最小复现加入自动测试。
6. 修改后运行完整 ctest，并对相关真实模型和所有生成正负样件做 after 回归。
7. 新建 `docs/iteration-29-results.md`（后续依次编号），记录技术点、效果、未改善项、报告路径、耗时和风险；同时更新本交接文件的基线状态。

可直接给新对话的提示语：

> 请先完整阅读《圆角识别算法基线与28轮迭代记录.md》。继续使用“识别圆角.cpp”作为原始 baseline，以 ImprovedFilletRecognizer 作为当前改进实现，不要恢复旧 FilletSearcher。先运行现有回归并生成待调整模型的 before 报告，再根据我提供的问题做第 29 轮改进；修改后必须运行完整测试、保留 after 报告，并记录本轮技术点、实际效果、未解决风险和性能变化。依赖、构建和报告继续放在 D 盘。

## 12. 结论

当前版本已经贯通：原始基线 A/B、自由/解析曲面候选、断裂支撑恢复、复杂分叉/闭环、变半径 R(s)、方向连续性、负样本、三态证据、真值评测、可审计报告和性能预算。它具备继续向工业量产推进的工程基础，但是否达到特定企业标准，只能由真实业务模型真值、Release 性能和宿主集成验收决定。
